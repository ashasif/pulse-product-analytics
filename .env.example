# Pulse Product Analytics
# Environment variable template
#
# Copy this file to .env when local credentials are required.
# Never commit real passwords or secrets.

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pulse_analytics
DB_USER=your_postgres_username
DB_PASSWORD=your_postgres_password
